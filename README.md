The to-do list app is built in Java and XML with SQLite database. 
The plus button at the bottom of the home screen is for adding a new task. 
Slide right on the task to delete it, and slide left to edit."
